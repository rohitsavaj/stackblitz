import { Routes } from '@angular/router';
import { RxjsRelated } from './components/rxjs-related/rxjs-related';

export const routes: Routes = [
    {
        path: 'rxjs',
        component: RxjsRelated
    }
];
